LANGSMITH_ENDPOINT=https://api.smith.langchain.com
LANGSMITH_PROJECT=chatgpt-app-tracing
LANGSMITH_API_KEY=

MCP_HOST=127.0.0.1
MCP_PORT=8765

# Only needed for model_harness.py, which drives the server with a real model.
OPENAI_API_KEY=
# Optional. Set to route model calls through a gateway or compatible endpoint.
OPENAI_BASE_URL=
# Optional. Defaults to gpt-4o-mini.
HARNESS_MODEL=
